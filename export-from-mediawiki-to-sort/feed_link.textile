\n"mininav&#95;tags":/home/www/zendstudio/dokuwiki/bin/lib/exe/fetch.php?id=&media=mininav_tags

bc(code). <txp:feed_link>


The "feed&#95;link":/home/www/zendstudio/dokuwiki/bin/doku.php?id=feed_link tag can be used as either a _single_ or _container_ tag and is used to output a link to the site's “articles” RSS feed. When used as a container tag, it will turn the contents into a link to the feed, otherwise the value of attribute “label” will be used as link text. Should be used in a page.

h3(sectionedit1#attributes). Attributes

Tag will accept the following attributes (*case-sensitive*):

*"category":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#category=“category name”*
 Restrict to specified category. Default: current category. *"flavor":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#flavor=“value”*
 Whether to output a link to the RSS or Atom version of the feed. Available values:

bc(code). rss


or

bc(code). atom


. Default:

bc(code). rss


. *"format":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#format=“value”*
 Whether to output an HTML "anchor":http://www.w3.org/TR/html4/struct/links.html#edef-A or "link":http://www.w3.org/TR/html4/struct/links.html#edef-LINK tag. Values:

bc(code). a


or

bc(code). link


. Default:

bc(code). a


. *"limit":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#limit=“integer”*
 Number of articles to display in the feed. Default: depends upon "preference setting":/home/www/zendstudio/dokuwiki/bin/doku.php?id=advanced_preferences#how_many_articles_on_rss_2. *"section":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#section=“section name”*
 Restrict to specified section. Default: current section. *"title":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#title=“value”*
 "HTML title attribute":http://www.w3.org/TR/html4/struct/global.html#adef-title. Default: depends upon

bc(code). flavor


used, either

bc(code). RSS feed


or

bc(code). Atom feed


.

"common&#95;attributes&#95;header":/home/www/zendstudio/dokuwiki/bin/lib/exe/fetch.php?id=&media=common_attributes_header

"attribute&#95;label":/home/www/zendstudio/dokuwiki/bin/lib/exe/fetch.php?id=&media=attribute_label "attributes&#95;wraptag&#95;class":/home/www/zendstudio/dokuwiki/bin/lib/exe/fetch.php?id=&media=attributes_wraptag_class

*Note:* "wraptag":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#wraptag is applicable only when using

bc(code). format


of

bc(code). a


.

h3(sectionedit2#examples). Examples

h4(#example_1display_an_rss_feed_link_for_specific_section_and_category). Example 1: Display an RSS feed link for specific section and category

bc(code). <txp:feed_link flavor="rss" section="about" category="general" label="XML" wraptag="p" />


h4(#example_2container_example). Example 2: Container example

bc(code). <txp:feed_link wraptag="p"><img src="/path/to/rss-icon" /></txp:feed_link>


h4(#example_3site_wide_generic_rss_feed). Example 3: Site wide generic RSS feed

bc(code). <txp:feed_link section="" category="" />


*What this does...*
 Create a link to the site's feed for articles in all sections and categories. If you omit the

bc(code). section


and

bc(code). category


attributes, the feed will default to the current section/category.

h4(#example_4with_symbolset_s_rss_glyph). Example 4: With Symbolset's 'rss' glyph

If you happen to use the “rss” glyph in the social media set of "Symbolset":http://symbolset.com/, you can still use this tag. Let's say you're creating a social button bar using Symbolset glyphs in a list. The normal way to do this would be to set up your selectors on the individual anchor elements, like the first three list items show below. But for the *rss* glyph you need to put the selectors in the

bc(code). li


, as the last list item shows:

bc(code). <ul class="socbar">
    <li><a href="https://twitter.com/xxx" class="ss-icon twit" title="">twitter</a></li>
    <li><a href="https://plus.google.com/xxx/posts" class="ss-icon gplus" title="">googleplus</a></li>
    <li><a href="http://www.linkedin.com/in/xxx" class="ss-icon in" title="Pffft">linkedin</a></li>
    <li class="ss-icon rss"><txp:feed_link flavor="rss" section="articles" category="" label="rss" /></li>
</ul>


*Notes:*

<ul>
<li><p>If you're using Symbolset, then you'll know that the</p>
bc(code). label=""


<p>attribute in the last list item above _must_ be “*rss*” for the glyph to work.</p></li>
<li><p>If you try and put the two Symbolset class attribute values in the "feed&#95;link":/home/www/zendstudio/dokuwiki/bin/doku.php?id=feed_link tag using its</p>
bc(code). class=""


<p>attribute, it won't work, unfortunately. But putting them in the</p>
bc(code). li


<p>element like shown above does.</p></li>
<li><p>Atom feed â€“ There is no “atom” trigger word in Symbolset! So while you can use</p>
bc(code). flavor="atom"


<p>and create an atom feed just fine, you still need to use</p>
bc(code). label="rss"


<p>for the link label to call the Symbolset glyph. This shouldn't be a problem because the glyph replaces the link text. You can then use</p>
bc(code). title=""


<p>to provide a custom hover text, or leave it out for the default display: “Atom feed”.</p></li>
</ul>

Then the CSS must be like follows to target both instances of Symbolset glyph use...

bc(code). /* 
****** First the common rules
*/
a.ss-icon,
li.ss-icon a { /* design as you want */ }
/*
****** Target each one if specific hover effect is wanted.
*/
.twit:hover { /* design as you want */ }
... etc ...
li.rss a:hover { /* design as you want */ }


*Tip:* See the "email":/home/www/zendstudio/dokuwiki/bin/doku.php?id=email tag for a similar solution for Symbolset's “email” glyph.

h3(sectionedit3#genealogy). Genealogy

h4(#version_430). Version 4.3.0

<ul>
<li>bc(code). class


<p>attribute added</p></li>
</ul>

h4(#version_404). Version 4.0.4

<ul>
<li>bc(code). format


<p>added.</p></li>
</ul>

