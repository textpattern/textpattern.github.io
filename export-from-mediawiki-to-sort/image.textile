\n"mininav&#95;tags":/home/www/zendstudio/dokuwiki/bin/lib/exe/fetch.php?id=&media=mininav_tags

bc(code). <txp:image />


The "image":/home/www/zendstudio/dokuwiki/bin/doku.php?id=image tag is a _single_ tag that Textpattern will replace with the

bc(code). <img src="..." />


HTML tag matching the image of the numeric

bc(code). id


assigned by Textpattern when the image was uploaded via the Textpattern "Images":/home/www/zendstudio/dokuwiki/bin/doku.php?id=images_panel tab.

h3(sectionedit1#attributes). Attributes

Tag will accept the following attributes (*case-sensitive*):

*"class":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#class=“class name”*
 CSS

bc(code). class


attribute applied to the

bc(code). wraptag


, if set, otherwise to the

bc(code). img


tag. Default: unset. *"escape":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#escape=“html”*
 Escape HTML entities such as

bc(code). <


,

bc(code). >


and

bc(code). &


for the image's

bc(code). alt


and

bc(code). title


attributes. Values:

bc(code). html


or unset. Default:

bc(code). html


. *"height":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#height=“integer”*
 Specify an image height which overrides the value stored in the database. Use

bc(code). height="0"


to turn off the output of a height attribute in the

bc(code). <img>


tag (thus the browser will scale the height if a width is used) Default: height of image stored in the database. *"html&#95;id":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#html_id=“id number”*
 The HTML

bc(code). id


attribute applied to the

bc(code). wraptag


, if set, otherwise to the

bc(code). img


tag. Default: unset. *"id":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#id=“integer”*
 Specifies the

bc(code). id


assigned at upload of the image to display. Can be found on the "Images":/home/www/zendstudio/dokuwiki/bin/doku.php?id=images_panel tab. If both

bc(code). name


and

bc(code). id


are specified,

bc(code). name


is used while

bc(code). id


is ignored. *"name":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#name=“image name”*
 Specifies which image to display by its image name as shown on the "Images":/home/www/zendstudio/dokuwiki/bin/doku.php?id=images_panel tab. *"style":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#style=“style rule”*
 Inline CSS style rule. Default: unset. *"width":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#width=“integer”*
 Specify an image width which overrides the value stored in the database. Use

bc(code). width="0"


to turn off the output of a width attribute in the

bc(code). <img>


tag (thus the browser will scale the width if a height is used) Default: width of image stored in the database. *"wraptag":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#wraptag=“tag text”*
 HTML tag to be used to wrap the

bc(code). img


tag, specified without brackets. Default is unset. *_align_[^deprecated^]=“HTML value”*
 HTML

bc(code). align


attribute for the

bc(code). img


tag. Recommended that you use CSS via

bc(code). class


or

bc(code). html_id


attribute instead.

h3(sectionedit2#examples). Examples

h4(#example_1display_the_given_image). Example 1: Display the given image

bc(code). <txp:image id="42" />


*What it does...*
 Displays the image uploaded as ID #42.

h4(#example_2apply_a_css_class). Example 2: Apply a CSS class

bc(code). <txp:image name="chickens.jpg" class="boxit" />


*What it does...*
 Displays the image named “chickens.jpg” and assigns a CSS class called

bc(code). boxit


to the

bc(code). <img>


tag.

*The style could be defined like this*

bc(code). img.boxit {
    background: #fff;
    border: 1px solid #ccc;
    display: block;
    margin: -5px 5px 5px -5px;
    padding: 4px;
    position: relative;
}


Had the

bc(code). wraptag


attribute been used, the

bc(code). boxit


class would have been applied to that instead of directly to the image.

h3(sectionedit3#genealogy). Genealogy

h4(#version_430). Version 4.3.0

<ul>
<li><p>attributes</p>
bc(code). width


<p>and</p>
bc(code). height


<p>added</p></li>
</ul>

h4(#version_420). Version 4.2.0

<ul>
<li><p>attribute</p>
bc(code). align


<p>deprecated</p></li>
</ul>

h4(#version_407). Version 4.0.7

<ul>
<li><p>default value for attribute</p>
bc(code). escape


<p>changed from unset to</p>
bc(code). html

</li>
</ul>

h4(#version_404). Version 4.0.4

* html&#95;id added
* escape added
* wraptag added

