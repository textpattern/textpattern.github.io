\n"mininav&#95;tags":/home/www/zendstudio/dokuwiki/bin/lib/exe/fetch.php?id=&media=mininav_tags

bc(code). <txp:category />


The "category":/home/www/zendstudio/dokuwiki/bin/doku.php?id=category tag can be used as either a _single_ or _containing_ tag. It will display information of the category as defined by the

bc(code). name


attribute, or the one currently being viewed. When used as a containing tag, it will turn the contents into a link to the category. Otherwise, it will return plain text.

May be used in any context.

h3(sectionedit1#attributes). Attributes

Tag will accept the following attributes (*case-sensitive*):

*"class":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#class=“class name”*
 "HTML class attribute":http://www.w3.org/TR/html4/struct/global.html#adef-class, applied to

bc(code). wraptag


. If no wraptag is supplied (and

bc(code). link="1"


), the class is applied to the anchor instead. Default: unset. *"link":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#link=“boolean” (works only in the _single_ tag)*
 Whether to display as link. Values:

bc(code). 1


(yes) or

bc(code). 0


(no). Default:

bc(code). 0


. *"name":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#name=“category name”*
 Display specific category. Note the category name is specified in lower case regardless of how you typed its title in the Category tab. Also note that if you had called your category *My Category Name* it becomes *my-category-name* when used in tags. Default: unset (use current category). *"section":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#section=“section name”*
 Restricts category search to named section. Default: current section (for backwards compatibility). *"this&#95;section":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#this_section=“boolean”*
 Only link to articles from the current section.

bc(code). section


attribute overrides this setting. Values:

bc(code). 1


(yes) or

bc(code). 0


(no). Default:

bc(code). 0


. *"title":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#title=“boolean”*
 Whether to display category's title instead of its name. Values:

bc(code). 1


(yes) or

bc(code). 0


(no, display name). Default:

bc(code). 0


. *"type":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#type=“category type”*
 Values:

bc(code). article


,

bc(code). image


,

bc(code). link


,

bc(code). file


. Default:

bc(code). article


. *"url":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#url=“boolean”*
 Display plain URL or full link. Values:

bc(code). 1


(yes) or

bc(code). 0


(no). Default:

bc(code). 0


(display title or full link, depending on

bc(code). link


) *"wraptag":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#wraptag=“tag”*
 "HTML tag":http://www.w3.org/TR/html4/intro/sgmltut.html#h-3.2.1 (without brackets) to wrap around output. Default: unset.

h3(sectionedit2#examples). Examples

h4(#example_1displays_the_current_category_name). Example 1: Displays the current category name

bc(code). <txp:category />


h4(#example_2display_hyperlinked_category_title). Example 2: Display hyperlinked category title

bc(code). <txp:category title="1" link="1" />


h4(#example_3display_a_specific_category_s_title_hyperlinked). Example 3: Display a specific category's title, hyperlinked

bc(code). <txp:category name="articles" title="1" link="1" wraptag="p" />


h4(#example_4container_example). Example 4: Container Example

bc(code). <txp:category name="book">My books</txp:category>


h3(sectionedit3#genealogy). Genealogy

h4(#version_407). Version 4.0.7

<ul>
<li><p>Applies</p>
bc(code). class


<p>attribute to the</p>
bc(code). <a>


<p>element when</p>
bc(code). wraptag


<p>is empty.</p></li>
<li><p>New attribute,</p>
bc(code). url


<p>to output URL only.</p></li>
</ul>

h4(#version_404). Version 4.0.4

<ul>
<li>bc(code). this_section


<p>added.</p></li>
</ul>

