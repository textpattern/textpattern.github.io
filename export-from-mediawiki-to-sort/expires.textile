\n"mininav&#95;tags":/home/www/zendstudio/dokuwiki/bin/lib/exe/fetch.php?id=&media=mininav_tags

bc(code). <txp:expires />


A single tag used to indicate when an article should no longer appear in a site, particularly when the information is date sensitive (e.g., events like conferences, meetings and so forth). The tag is defined by expiration date values that are set under the *More* section of the "write":/home/www/zendstudio/dokuwiki/bin/doku.php?id=write admin-side panel.

For more on this tag see its announcement, "So, youâ€™d like to stick a â€œBest Beforeâ€? label on those articles?":http://textpattern.com/weblog/327/so-youd-like-to-stick-a-best-before-label-on-those-articles

h3(sectionedit1#attributes). Attributes

Tag will accept the following attributes (*case-sensitive*):

*"class":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#class=“class name”*
 CSS class name which will be applied to the

bc(code). wraptag


element. Default: unset. *"format":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#format=“format string”*
 Override the default date format set in the preferences. Values: any valid "strftime()":http://php.net/strftime string values, or

bc(code). since


. Default: unset (date format set via preferences). *"gmt":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#gmt=“boolean”*
 Return either local time  -  according to the set time zone preferences  -  or GMT. Values:

bc(code). 0


(local time) or

bc(code). 1


(GMT). Default:

bc(code). 0


. *"lang":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#lang=“ISO language code”*
 Format time string suitable for the specified language (locale). Values: locales adhere to "ISO-639":http://www.loc.gov/standards/iso639-2/php/code_list.php. Default: unset (time format set via preferences). *"wraptag":/home/www/zendstudio/dokuwiki/bin/doku.php?id=attributes_cross-reference#wraptag=“tag”*
 HTML tag surrounding the expiry date, without brackets. Default: unset.

h3(sectionedit2#examples). Examples

h4(#example_1custom_format_date_setting). Example 1: Custom format date setting

bc(code). <p>Expires: <txp:expires format="%b %d, %Y" /></p>


would result in:

bc(code). <p>Expires: Sep 10, 2010</p>


h3(sectionedit3#genealogy). Genealogy

h4(#version_407). Version 4.0.7

* tag added.

h3(sectionedit4#related). Related

Related tags are:

* "if&#95;expires":/home/www/zendstudio/dokuwiki/bin/doku.php?id=if_expires
* "if&#95;expired":/home/www/zendstudio/dokuwiki/bin/doku.php?id=if_expired

